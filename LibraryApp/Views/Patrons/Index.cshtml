@{ 
  Layout = "_Layout";
}
@using LibraryApp.Models;

<h1>Patrons::</h1>
@if (@Model.Count == 0)
{
  <h3>No patrons to speak of</h3>
}
else
@foreach (Patron patron in Model)
{
 @Html.ActionLink($"{patron.Name}", "Details", new { id = patron.PatronId })
  <br>
}
<p>@Html.ActionLink("Back to list", "Home", "Index")</p>
<p>@Html.ActionLink("Add a Patron", "Create")