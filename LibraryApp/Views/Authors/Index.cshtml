@{ 
  Layout = "_Layout";
}
@using LibraryApp.Models;

<h1>Authors:</h1>
@if (@Model.Count == 0)
{
  <h3>No authors to speak of</h3>
}
else
@foreach (Author author in Model)
{
 @Html.ActionLink($"{author.Name}", "Details", new { id = author.AuthorId })
  <br>
}
<p>@Html.ActionLink("Back to list", "Home", "Index")</p>